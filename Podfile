platform :ios, '17.5'

target 'OpenKcal' do
  use_frameworks!
  
  # 여기에 필요한 CocoaPods 라이브러리 추가
  pod 'DropDown'  # 예시: 삭제된 DropDown 다시 추가
  
  target 'OpenKcalTests' do
    inherit! :search_paths
  end

  target 'OpenKcalUITests' do
    inherit! :search_paths
  end
end
